services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-news_system}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: docker/backend.Dockerfile
    env_file:
      - .env
      - news-backend/.env
    environment:
      DATABASE_URL: "${DATABASE_URL:-postgresql://postgres:postgres@postgres:5432/news_system}"
      NEWS_BASE_DIR: /app/news-backend
    volumes:
      - ./downloads:/app/news-backend/downloads
      - ./output:/app/news-backend/output
      - ./logs:/app/news-backend/logs
      - ./images:/app/news-backend/images
      - ./news-backend/system_config.json:/app/news-backend/system_config.json
      - ./collectors_config.json:/app/news-backend/collectors_config.json
      - ./scienceai_subscribers.json:/app/news-backend/scienceai_subscribers.json
      - ./news-backend/loop_stats.json:/app/news-backend/loop_stats.json
      - ./articles_registry.json:/app/news-backend/articles_registry.json
    ports:
      - "3005:3005"
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  dashboard:
    environment:
      NEWS_BASE_DIR: /data
      VITE_API_URL: http://backend:3005
    volumes:
      - ./output:/data/output:ro
      - ./images:/data/images:ro
      - ./articles_registry.json:/data/articles_registry.json:ro
    build:
      context: .
      dockerfile: docker/frontend.Dockerfile
      args:
        APP_DIR: news-dashboard
    depends_on:
      - backend
    ports:
      - "1420:80"
    restart: unless-stopped

  airesearch:
    environment:
      NEWS_BASE_DIR: /data
      BACKEND_URL: http://backend:3005
    volumes:
      - ./output:/data/output:ro
      - ./images:/data/images:ro
      - ./articles_registry.json:/data/articles_registry.json:ro
    build:
      context: .
      dockerfile: docker/frontend.next.Dockerfile
      args:
        APP_DIR: apps/frontend-next/AIResearch
    depends_on:
      - backend
    ports:
      - "3003:80"
    restart: unless-stopped

  scienceai:
    environment:
      NEWS_BASE_DIR: /data
      VITE_API_URL: http://backend:3005
    volumes:
      - ./output:/data/output:ro
      - ./images:/data/images:ro
      - ./scienceai_subscribers.json:/data/scienceai_subscribers.json
      - ./articles_registry.json:/data/articles_registry.json:ro
    build:
      context: .
      dockerfile: docker/scienceai.Dockerfile
    depends_on:
      - backend
    ports:
      - "8080:80"
    restart: unless-stopped

networks:
  default:
    name: news-system

volumes:
  postgres_data:
    driver: local

